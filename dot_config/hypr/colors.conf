# chezmoi source: dot_config/hypr/colors.conf
# chezmoi destination: ~/.config/hypr/colors.conf
# Ricer's Log:
# 1. This fallback keeps Hyprland reloadable before matugen has ever run.
# 2. Variable names mirror the Velvet Noir palette exactly.
# 3. Matugen can replace this file without touching the core Hyprland config.

$base = rgb(0d0d0f) # matugen: this value is overridden by matugen on wallpaper change.
$surface = rgb(131316) # matugen: this value is overridden by matugen on wallpaper change.
$overlay = rgb(1c1c21) # matugen: this value is overridden by matugen on wallpaper change.
$muted = rgb(3a3a45) # matugen: this value is overridden by matugen on wallpaper change.
$subtle = rgb(6b6b7a) # matugen: this value is overridden by matugen on wallpaper change.
$text = rgb(e8e6f0) # matugen: this value is overridden by matugen on wallpaper change.
$accent = rgb(b48eff) # matugen: this value is overridden by matugen on wallpaper change.
$accent2 = rgb(7dd3c8) # matugen: this value is overridden by matugen on wallpaper change.
$accent3 = rgb(f0a5c0) # matugen: this value is overridden by matugen on wallpaper change.
$red = rgb(f28b7d) # matugen: this value is overridden by matugen on wallpaper change.
$green = rgb(8ecfb0) # matugen: this value is overridden by matugen on wallpaper change.a
$yellow = rgb(f5c97a) # matugen: this value is overridden by matugen on wallpaper change.

# End Ricer's Log:
# - A committed fallback prevents first-boot missing-include friction.
# - Values stay palette-pure.
# - Generated color files remain small and auditable.
